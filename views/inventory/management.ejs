<% if (title) { %>
    <h1> <%= title %> </h1>
    <% } else {
      res.redirect('/')
    } %>

<% if (errors) { %>
    <ul class="notice">
   <% errors.array().forEach(error => { %>
     <li><%= error.msg %></li>
  <%  }) %>
   </ul>
  <% } %>

<%- messages() %>

<body>
<p><a class="add" href="/inv/addClassification">Add New Classification</a></p>
<p><a class="add" href="/inv/addInventory">Add New Inventory</a></p>
<p><a class="add" href="/inv/editInventory">Edit Inventory</a></p>

<h2>Manage Inventory</h2>
<p>Choose a classification to see inventory items.</p>
<%- classificationSelect %>

<table id="inventoryDisplay"></table>

<noscript>JavaScript must be enabled to use this page.</noscript>

<script src="../../js/inventory.js"></script>

</body>